# Windows Setup Instructions for InfraWatch

## Быстрый старт (3 шага)

### 1. Скачайте проект
```bash
git clone https://github.com/onneyami/InfraWatch.git
cd InfraWatch
```

### 2. Запустите setup скрипт
**Вариант A: Двойной клик (Рекомендуется)**
- Найдите файл `setup-windows.bat` в папке проекта
- Щёлкните по нему двойной клик
- Система может запросить подтверждение администратора - нажмите "Да"

**Вариант B: Через PowerShell (Альтернативный способ)**
- Откройте PowerShell от имени администратора
- Перейдите в папку проекта: `cd C:\путь\к\InfraWatch`
- Запустите: `Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process`
- Затем: `.\setup-windows.ps1`

### 3. Начните разработку
После завершения setup скрипта выберите один из способов:

**Способ 1: Используя Makefile (если установлен Make)**
```bash
make start
```

**Способ 2: Запуск компонентов отдельно**

Откройте 2 терминала:

**Терминал 1 - Фронтенд:**
```bash
cd frontend
npm run dev
```

**Терминал 2 - Бэкенд:**
```bash
cd backend
python -m uvicorn src.main:app --reload
```

## Что установится?

### Системные инструменты:
- ✅ **Chocolatey** - менеджер пакетов для Windows
- ✅ **Node.js & npm** - для фронтенда (React, TypeScript, Vite)
- ✅ **Python** - для бэкенда (FastAPI, Docker SDK)
- ✅ **Git** - контроль версий
- ✅ **Make** - автоматизация сборки
- ✅ **Docker Desktop** - контейнеризация (опционально)

### VS Code расширения:
- ✅ Python & Pylance - разработка на Python
- ✅ ESLint & Prettier - форматирование кода
- ✅ Tailwind CSS - подсказки для стилей
- ✅ Docker - работа с контейнерами
- ✅ GitLens - интеграция Git
- ✅ и другие...

### Зависимости проекта:
- ✅ `frontend/node_modules` - установятся автоматически
- ✅ Python пакеты из `backend/requirements.txt` - установятся в виртуальное окружение

## Требования

- **ОС:** Windows 10 или Windows 11
- **Права:** Администратор (для установки программ)
- **Интернет:** Требуется (для скачивания пакетов)
- **Место на диске:** ~5-10 GB
- **RAM:** Минимум 4 GB (рекомендуется 8 GB)

## Если что-то пошло не так

### Проблема: "PowerShell не найден"
**Решение:** PowerShell уже встроен в Windows 10/11. Убедитесь, что используете встроенный PowerShell, а не другую оболочку.

### Проблема: "Доступ запрещён"
**Решение:** Убедитесь, что запускаете скрипт от имени администратора.

### Проблема: "Команда 'make' не найдена"
**Решение:** Это нормально - используйте Способ 2 для запуска компонентов.

### Проблема: "npm install не работает"
**Решение:** Закройте все окна PowerShell, перезагрузитесь и попробуйте снова.

### Проблема: "Python не найден"
**Решение:** После установки закройте и снова откройте PowerShell, чтобы PATH обновился.

## Запуск после первой установки

На следующих разах просто:

```bash
cd frontend
npm run dev          # В одном терминале

# В другом терминале
cd backend
python -m uvicorn src.main:app --reload
```

## VS Code

После установки откройте папку проекта в VS Code:
```bash
code .
```

Все расширения будут уже установлены!

## Docker (опционально)

Если хотите использовать Docker:
1. Установите Docker Desktop вручную: https://www.docker.com/products/docker-desktop
2. Запустите Docker Desktop перед работой с контейнерами
3. Используйте `docker` команды в терминале

## Дополнительная информация

Для детальной информации о проекте см. **README.md** в корневой папке.

---

**Успешного развития! 🚀**
